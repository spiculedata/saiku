<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.saikuanalytics</groupId>
        <artifactId>saiku</artifactId>
        <version>4.3.6</version>
    </parent>

    <artifactId>saiku-launcher</artifactId>
    <packaging>jar</packaging>
    <name>saiku - launcher</name>
    <description>Executable Saiku distribution: Picocli CLI + embedded Jetty serving the Saiku webapp.</description>

    <properties>
        <jetty.version>12.0.32</jetty.version>
        <picocli.version>4.7.6</picocli.version>
        <maven.compiler.release>21</maven.compiler.release>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.saikuanalytics</groupId>
            <artifactId>saiku-webapp</artifactId>
            <version>${project.version}</version>
            <type>war</type>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
            <version>${picocli.version}</version>
        </dependency>

        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-server</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jetty.ee10</groupId>
            <artifactId>jetty-ee10-webapp</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jetty.ee10</groupId>
            <artifactId>jetty-ee10-annotations</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>1.7.36</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Integration test dependencies. JUnit + Jackson for JSON shape
             assertions. JDK's java.net.http handles the wire calls. -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.18.6</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>saiku-${project.version}</finalName>
        <plugins>
            <!-- The launcher artifact is a ~360MB Spring Boot fat jar:
                 a runnable thing, not a library. Nobody Maven-depends on
                 a fat jar. Skip mvn deploy so it stays a GitHub release
                 asset (downloadable from the Releases page) rather than
                 also landing in GH Packages as a multi-hundred-meg blob. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.3</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <execution>
                        <id>copy-war</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.saikuanalytics</groupId>
                                    <artifactId>saiku-webapp</artifactId>
                                    <version>${project.version}</version>
                                    <type>war</type>
                                    <outputDirectory>${project.build.outputDirectory}/webapp</outputDirectory>
                                    <destFileName>saiku.war</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
              Integration tests boot the launcher's Jetty in-process via
              SaikuLauncher.ServeCommand.bootServer(...). Bound to the
              integration-test phase so they run AFTER prepare-package
              stages target/classes/webapp/saiku.war via the copy-war
              execution below. Gated by the {@code integration} profile
              (see below) so `mvn verify` keeps the inner dev loop fast.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.5.2</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <includes>
                        <include>**/*IT.java</include>
                    </includes>
                    <!-- Real OLAP queries are heavyweight; ITs run serially. -->
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    <argLine>--add-opens=java.base/java.nio=ALL-UNNAMED</argLine>
                    <!-- Default skip; the {@code integration} profile flips this on. -->
                    <skipITs>true</skipITs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                                <!-- xerces/xalan are transitively pulled by
                                     mondrian + hadoop (via hive-jdbc) and
                                     hijack JAXP via META-INF/services
                                     overrides. With Spring 6.2's stricter
                                     XML namespace validation, the
                                     applicationContext.xml's
                                     <mvc:annotation-driven/> element fails
                                     XSD lookup against the xerces parser
                                     because Spring's classpath EntityResolver
                                     isn't consulted by xerces's external
                                     entity resolution. JDK 21 has internal
                                     xerces+xalan that respects JAXP's
                                     standard resolver chain — drop the
                                     external ones from the fat JAR so the
                                     JDK's implementation wins. -->
                                <filter>
                                    <artifact>xerces:xercesImpl</artifact>
                                    <excludes>
                                        <exclude>**</exclude>
                                    </excludes>
                                </filter>
                                <filter>
                                    <artifact>xalan:xalan</artifact>
                                    <excludes>
                                        <exclude>**</exclude>
                                    </excludes>
                                </filter>
                                <filter>
                                    <artifact>xalan:serializer</artifact>
                                    <excludes>
                                        <exclude>**</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>org.saiku.launcher.SaikuLauncher</mainClass>
                                    <manifestEntries>
                                        <Multi-Release>true</Multi-Release>
                                        <!-- Apache Arrow (used by ArrowCellsetWriter) needs reflective
                                             access to java.nio.DirectByteBuffer's "address" field on
                                             Java 17+. Without this, MemoryUtil.<clinit> fails and the
                                             very first MDX query returns 500. -->
                                        <Add-Opens>java.base/java.nio</Add-Opens>
                                    </manifestEntries>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Activate with `mvn verify -P integration` to actually run the
             whole-API integration tests. The default reactor (CI's
             `mvn verify`) leaves them skipped so day-to-day builds stay fast. -->
        <profile>
            <id>integration</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <configuration>
                            <skipITs>false</skipITs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
